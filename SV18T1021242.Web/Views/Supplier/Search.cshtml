@model SV18T1021242.Web.Models.SupplierPaginationResult

@{
    Layout = null;
}

<p style="margin-top:10px">
    Có <b>@Model.RowCount</b> nhà cung cấp trong tổng số <strong>@Model.PageCount</strong> trang
</p>
<div class="table-responsive">
    <table class="table table-bordered table-striped table-hover">
        <thead>
            <tr class="bg-blue-gradient">
                <th>Tên Nhà Cung Cấp</th>
                <th>Tên Giao Dịch</th>
                <th>Địa Chỉ</th>
                <th>Quốc Gia</th>
                <th>Thành Phố</th>
                <th>Mã Bưu Chính</th>
                <th>Số điện thoại</th>
                <th style="width: 110px"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Data)
            {
                <tr>
                    <td>@item.SupplierName</td>
                    <td>@item.ContactName</td>
                    <td>@item.Address</td>
                    <td>@item.Country</td>
                    <td>@item.City</td>
                    <td>@item.PostalCode</td>
                    <td>@item.Phone</td>
                    <td>

                        <a href="~/supplier/edit/@item.SupplierID" class="btn btn-info"><i class="fa fa-edit"></i></a>
                        <a href="~/supplier/delete/@item.SupplierID" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                    </td>
                </tr>

            }

        </tbody>
    </table>
</div>
<div class="box-footer">
    <div class="text-center">
        <ul class="pagination">
            @for (int i = 1; i <= Model.PageCount; i++)
            {
                if (i == Model.Page)
                {
                    <li class="active"><a href="~/supplier">@i</a></li>
                }
                else
                {
                    <li><a href="javascript:;" onclick="doSearch(@i)">@i</a></li>
                }
            }
        </ul>
    </div>
</div>