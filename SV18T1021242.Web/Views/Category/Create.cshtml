@model SV18T1021242.DomainModel.Category
@{
    
}

<div class="box box-primary">
    <div class="box-header with-border">

    </div>
    <div class="box-body">
        <form class="form-horizontal" action="~/category/save" method="post">
            <input type="hidden" name="CategoryID" value="@Model.CategoryID" />

            <div class="form-group">
                <label class="control-label col-sm-2">Tên Loại Hàng</label>
                <div class="col-sm-10">
                    @Html.TextBoxFor(c => c.CategoryName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(c => c.CategoryName, "", new { @style = "color:#ff0000" })

                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Miêu Tả</label>
                <div class="col-sm-10">   
                        @Html.TextBoxFor(c => c.Description, new { @class = "form-control" })
                        @Html.ValidationMessageFor(c => c.Description, "", new { @style = "color:#ff0000" })
   
                    @*<select class="form-control"
                id="Description"
                name="Description">
            @foreach (var item in SV18T1021242.BusinessLayer.CommonDataService.ListOfDescriptions())
            {
                if (item.Description == Model.Description)
                {
                    <option value="@item.Description" selected>@item.Description</option>
                }
                else
                {
                    <option value="@item.Description">@item.Description</option>
                }

            }

        </select>*@
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-primary"><i class="fa fa-save" style="margin-right:5px"></i>Lưu dữ liệu</button>
                    <a href="~/category" class="btn btn-default"><i class="fa fa-chevron-left" style="margin-right:5px"></i>Quay lại</a>
                </div>
            </div>
        </form>
    </div>

</div>

